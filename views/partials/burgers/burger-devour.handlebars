<div class="mt-3">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>

	<button class="delete-burger btn text-white" data-id="{{this.id}}"><i class="far fa-trash-alt mr-2"></i>Throw away</button>
</div>